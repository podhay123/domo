#include <iostream> 
using namespace std; 
 
int main(){ 
    int n;
    cin >> n;
    while (n != 0){
        int array_len;
        cin >> array_len;
        int i = 0;
        int array[array_len];
        while (i != array_len){
            int num;
            cin >> num;
            array[i] = num;
            i++;
        }
        
        char first_char;
        cin >> first_char
        while (first_char != "F"){

        }
    }


}